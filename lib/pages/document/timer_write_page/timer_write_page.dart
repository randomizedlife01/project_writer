import 'package:flutter/material.dart';

class TimerWritePage extends StatelessWidget {
  final String appBarTitle;

  TimerWritePage({Key key, this.appBarTitle});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
